---
description: List and search session bookmarks with cbm
argument-hint: "[query]"
allowed-tools: Bash(cbm:*)
---

Run this command and show its output to the user unchanged:

```
cbm list $ARGUMENTS
```

To reopen a bookmark, tell the user to run `cbm open <name>` in a terminal.
